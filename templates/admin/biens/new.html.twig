{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-5">Ajouter un nouveau bien</h1>
        {{ include('admin/biens/_form.html.twig', {button: 'Ajouter'}) }}
        <div class="text-center">
            <a href="{{ path('admin.biens.index') }}" class="btn btn-secondary">Retour</a>
        </div>
    </div>
{% endblock %}